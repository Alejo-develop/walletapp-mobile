import { Dimensions } from "react-native";

export const {width} = Dimensions.get('screen')

export const {height} = Dimensions.get('screen')

export const primaryColor = '#151515'

export const secondaryColor = '#D6FF00'