import {Text, View} from 'react-native';

const LoginScreen = () => {
  return (
    <View>
      <Text style={{color: 'black'}}>Hola desde login</Text>
    </View>
  );
};

export default LoginScreen;
